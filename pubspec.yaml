name: things
description: things

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  scroll_snap_list: ^0.9.1
  flutter_bloc: ^8.1.3

dev_dependencies:
  build_runner:
  flutter_gen_runner: ^5.3.2

  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/json/things.json